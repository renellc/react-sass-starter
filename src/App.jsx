import React from "react";
import "./App.scss";

const App = () => {
    return (
        <div>
            <h1>Hello, World!</h1>
            <p>This is a starter React project with SASS support configured.</p>
        </div>
    );
};

export default App;
